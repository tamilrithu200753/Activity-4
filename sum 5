def hammingWeight(n):
    count = 0
    while n:
        count += n & 1  # check if last bit is 1
        n >>= 1         # shift right by 1
    return count

# Example usage
n = 11  # binary 1011
print(hammingWeight(n))  # Output: 3
